export default class Pharmacy {
  constructor(id, pharmacistName, hourlyRate, reviews) {
    this.id = id;
    this.pharmacistName = pharmacistName;
    this.hourlyRate = hourlyRate;
    this.reviews = reviews;
  }
}
